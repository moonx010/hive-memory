{
  "name": "hive-memory",
  "version": "3.0.0",
  "description": "Cross-project memory layer for AI coding agents — MCP server that maintains context, decisions, and knowledge across workspaces",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "hive-memory": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "build:native": "cd native && npm install && npm run build",
    "build:all": "npm run build:native && npm run build",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "eval:enrichment": "npx tsx src/enrichment/eval/eval.ts",
    "eval:decisions": "npx tsx src/enrichment/eval/decision-eval.ts"
  },
  "keywords": [
    "mcp",
    "memory",
    "ai-agent",
    "claude-code",
    "claude",
    "codex",
    "knowledge-management",
    "cross-project",
    "context",
    "model-context-protocol"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/moonx010/hive-memory.git"
  },
  "homepage": "https://github.com/moonx010/hive-memory#readme",
  "bugs": {
    "url": "https://github.com/moonx010/hive-memory/issues"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^12.8.0",
    "sqlite-vec": "^0.1.7",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^10.0.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.56.1",
    "vitest": "^3.0.0"
  }
}
